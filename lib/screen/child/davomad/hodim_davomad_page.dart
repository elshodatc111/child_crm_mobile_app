import 'package:flutter/material.dart';
class HodimDavomadPage extends StatelessWidget {
  const HodimDavomadPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hodimlar davomadi"),),
    );
  }
}
